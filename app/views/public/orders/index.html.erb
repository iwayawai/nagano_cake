<h3>注文履歴一覧</h3>

<h4>注文日</h4>
<h4>配送先</h4>
<h4>注文商品</h4>
<h4>支払金額</h4>
<h4>ステータス</h4>
<h4>注文詳細</h4>

<%= @order_items.each do |order_item| %>
<%= order_item.created_at.strftime('%Y/%m/%d') %>
<%= order_item.order.address %>
<%= order_item.item.name %>
<%= order_item.item.add_tax_price %>
<%= order_item.order.status ? '入金待ち':'入金確認':'製作中':'発送準備中':'発送済み' %>
<%= link_to order_path(order.id) do %>
表示する
<% end %>