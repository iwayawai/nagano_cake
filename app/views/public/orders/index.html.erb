<%= render 'public/public' %>
<h3>注文履歴一覧</h3>

<h4>注文日</h4>
<h4>配送先</h4>
<h4>注文商品</h4>
<h4>支払金額</h4>
<h4>ステータス</h4>
<h4>注文詳細</h4>

<%= @orders.each do |order| %>
<%= order.created_at.strftime('%Y/%m/%d') %>
<%= order.address %>

<%= order.order_items.each do |order_item| %>
<%= order_item.item.name %>
<% end %>

<%= order.add_tax_price.to_s(:delimited) %>
<%= order.status %>
<%= link_to order_path(order.id) do %>
表示する
<% end %>
<% end %> 

<%= paginate @orders %>